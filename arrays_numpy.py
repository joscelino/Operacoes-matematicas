import numpy as np

a = np.zeros(10, float)
b = np.arange(0, 5, 1)
c = np.linspace(5, 25, 10)
d = np.empty(20, float)

m = np.zeros([2, 3], float)
m1 = np.array([[1, 2, 3], [4, 5, 6]], int)

list_1 = np.arange(0, 9)
matrix = np.reshape(list_1, (3, 3))
